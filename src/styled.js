import styled from "styled-components";

export const DivIconHero = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  width: clamp(36px, 13vw, 60px);
  height: clamp(36px, 13vw, 60px);
  border-radius: 50%;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
`;
